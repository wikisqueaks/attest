% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cite.R
\name{att_cite}
\alias{att_cite}
\title{Generate a BibTeX Citation}
\usage{
att_cite(source, store = NULL, key = NULL, write = TRUE)
}
\arguments{
\item{source}{An \code{\link[=att_source]{att_source()}} object or a source name (character). When a
name is given, metadata is read from the provenance record.}

\item{store}{Path to the provenance store. Defaults to \code{\link[=att_store]{att_store()}}.}

\item{key}{BibTeX citation key. Defaults to a sanitized version of the
source name.}

\item{write}{Logical; if \code{TRUE} (default), write the entry to
\code{data-sources.bib} in the store root.}
}
\value{
The BibTeX entry as a character string, invisibly.
}
\description{
Creates a BibTeX entry for a data source using its metadata and provenance
record. The access date, URL, and file hashes are populated automatically.
}
\details{
When \code{write = TRUE}, the entry is appended to \code{data-sources.bib} in the
store root directory. If an entry with the same key already exists, it is
replaced.
}
\examples{
\dontrun{
att_cite("my-source")
}
}
