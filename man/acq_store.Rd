% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{acq_store}
\alias{acq_store}
\title{Get or Set the Active Store Path}
\usage{
acq_store(path = NULL)
}
\arguments{
\item{path}{If provided, sets this as the active store path (via
\code{options(acquire.store = path)}). If \code{NULL}, returns the current store
path.}
}
\value{
The store path (character), invisibly when setting.
}
\description{
Retrieves or sets the path to the directory where acquired data sources are
stored. Resolution order: explicit \code{path} argument, then \code{acquire.store}
option, then \code{ACQUIRE_STORE} environment variable, then \code{"data/raw"} in the
current working directory.
}
\examples{
# Get the current store path
acq_store()

# Set a custom store path
acq_store("/path/to/my/store")
}
