% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-metadata.R
\name{att_add_metadata}
\alias{att_add_metadata}
\title{Add Metadata Files to an Existing Source}
\usage{
att_add_metadata(source, urls, store = NULL)
}
\arguments{
\item{source}{A source name (character) or \code{\link[=att_source]{att_source()}} object.}

\item{urls}{A character vector of metadata URLs to download. Named elements
use the name as the local filename; unnamed elements derive the filename
from the URL.}

\item{store}{Path to the provenance store. Defaults to \code{\link[=att_store]{att_store()}}.}
}
\value{
The updated provenance record, invisibly.
}
\description{
Downloads one or more metadata files and adds them to an already-attested
source. The new files are placed in the \verb{metadata/} subdirectory and the
provenance record is updated. Existing files are not modified.
}
\examples{
\dontrun{
att_add_metadata("my-source", "https://example.com/codebook.pdf")

att_add_metadata(
  "my-source",
  c(codebook = "https://example.com/codebook.pdf",
    schema   = "https://example.com/schema.xml")
)
}
}
