% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{att_link}
\alias{att_link}
\title{Link a Remote Source Without Downloading}
\usage{
att_link(source, store = NULL, cite = TRUE)
}
\arguments{
\item{source}{An \code{\link[=att_source]{att_source()}} object. Must have at least one of
\code{data_urls}, \code{metadata_urls}, or \code{landing_url} set. Must not have local
paths (\code{data_paths}/\code{metadata_paths}).}

\item{store}{Path to the provenance store. Defaults to \code{\link[=att_store]{att_store()}}.}

\item{cite}{Logical; if \code{TRUE} (default), generate a BibTeX citation and
append it to \code{data-sources.bib} in the store root.}
}
\value{
A list containing the full provenance record, invisibly.
}
\description{
Records provenance for a remote data source that is accessed live — a REST
API, a data portal query, or any URL you call rather than download a file
from. Creates the standard store directory structure and writes a
\code{provenance.json} record with \code{origin = "link"}, but downloads nothing.
}
\details{
A \code{.url} Internet Shortcut file is written to the source directory so the
endpoint is easy to open directly from a file browser.

An HTTP HEAD request is attempted against the primary URL (first of
\code{data_urls}, or \code{landing_url} if no \code{data_urls} are set) to record the
HTTP status and content type at link time. This is best-effort: if the
request fails, a warning is issued but the provenance record is still
written.

\code{att_link()} is intended for first-time registration. If a provenance
record already exists for this source, it will error and recommend
\code{\link[=att_update]{att_update()}} instead.
}
\examples{
\dontrun{
src <- att_source(
  name = "earthquake-feed",
  data_urls = "https://earthquake.usgs.gov/fdsnws/event/1/query?format=geojson",
  landing_url = "https://earthquake.usgs.gov/fdsnws/event/1/",
  title = "USGS Earthquake Feed",
  publisher = "U.S. Geological Survey",
  year = "2026"
)
att_link(src)
}
}
