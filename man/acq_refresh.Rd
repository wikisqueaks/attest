% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh.R
\name{acq_refresh}
\alias{acq_refresh}
\title{Refresh a Source}
\usage{
acq_refresh(source, store = NULL, archive = TRUE)
}
\arguments{
\item{source}{A source name (character) or \code{\link[=acq_source]{acq_source()}} object.}

\item{store}{Path to the provenance store. Defaults to \code{\link[=acq_store]{acq_store()}}.}

\item{archive}{Logical; if \code{TRUE} (default), archive current files before
updating.}
}
\value{
A data frame summarising the comparison (columns: \code{file}, \code{status},
\code{old_hash}, \code{new_hash}, \code{old_size}, \code{new_size}), invisibly.
}
\description{
Re-fetches all files for a previously acquired source, compares them
against the recorded hashes, and updates the local store only if changes are
detected. Optionally archives the current files before overwriting.
}
\details{
For remote sources (\code{origin = "remote"}), files are re-downloaded from their
original URLs. For local sources (\code{origin = "local"}), files are re-read
from their original \code{source_path}.
}
